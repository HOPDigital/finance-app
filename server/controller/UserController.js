const router = require('express').Router();
const AppErrors = require('../services/AppErrors')

// Get user
router.get('/', (req, res) => { })

// Create user
router.post('/', (req, res) => { })

// Update user
router.patch('/', (req, res) => { })

// Delete user
router.delete('/', (req, res) => { })